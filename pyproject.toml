[project]
name = "tanik"
version = "0.1.0"
description = "tanik game!"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
]

[project.scripts]
tanik = "tanik.main:main"
